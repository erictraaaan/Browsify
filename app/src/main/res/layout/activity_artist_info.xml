<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_artist_info"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="8dp"
    android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="8dp"
    tools:context="tran.eric.browsify.ArtistInfo">

    <TextView
        android:id="@+id/chosen_artist_name"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:textSize="24dp"
        tools:text="test artist name"
        android:textAlignment="center"
        android:background="#FFFFFF"
        android:elevation="5dp"
        />

    <ListView
        android:id="@+id/chosen_artist_album_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/chosen_artist_name"/>

</RelativeLayout>
